#!/bin/bash

# exit on error
set -e

apt update

# Clone the gpu-burn repository
cd
git clone https://github.com/wilicc/gpu-burn.git

# Change directory into gpu-burn
cd gpu-burn

# Build the gpu-burn tool
make


echo 'setup done'
